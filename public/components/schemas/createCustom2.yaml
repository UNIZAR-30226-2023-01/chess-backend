type: object
properties:
  gameType:
    type: string
    enum: 
      - AI
      - COMPETITIVE
      - CREATE CUSTOM
      - JOIN CUSTOM
  roomID:
    type: string
    description: Id of the room another player has created.
